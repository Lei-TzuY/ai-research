# Imported projects

This directory is reserved for verified history-preserving imports. A project listed in `manifest.json` is not considered imported until its status and subtree evidence say so explicitly.
